<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.gstatic.com/" crossorigin="" />
    <link
      rel="stylesheet"
      as="style"
      onload="this.rel='stylesheet'"
      href="https://fonts.googleapis.com/css2?display=swap&family=Inter:wght@400;500;700;900&family=Noto+Sans:wght@400;500;700;900"
    />
    <title>MediCare - Receptionist Dashboard</title>
    <link rel="icon" type="image/x-icon" href="data:image/x-icon;base64," />
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/dashboard.css?v=debug2024">
  </head>
  <body>
    <div class="dashboard-root">
      <div class="dashboard-container">
        <header class="dashboard-header">
          <div class="dashboard-header-left">
            <div class="dashboard-header-icon">
              <svg viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path
                  fill-rule="evenodd"
                  clip-rule="evenodd"
                  d="M39.475 21.6262C40.358 21.4363 40.6863 21.5589 40.7581 21.5934C40.7876 21.655 40.8547 21.857 40.8082 22.3336C40.7408 23.0255 40.4502 24.0046 39.8572 25.2301C38.6799 27.6631 36.5085 30.6631 33.5858 33.5858C30.6631 36.5085 27.6632 38.6799 25.2301 39.8572C24.0046 40.4502 23.0255 40.7407 22.3336 40.8082C21.8571 40.8547 21.6551 40.7875 21.5934 40.7581C21.5589 40.6863 21.4363 40.358 21.6262 39.475C21.8562 38.4054 22.4689 36.9657 23.5038 35.2817C24.7575 33.2417 26.5497 30.9744 28.7621 28.762C30.9744 26.5497 33.2417 24.7574 35.2817 23.5037C36.9657 22.4689 38.4054 21.8562 39.475 21.6262ZM4.41189 29.2403L18.7597 43.5881C19.8813 44.7097 21.4027 44.9179 22.7217 44.7893C24.0585 44.659 25.5148 44.1631 26.9723 43.4579C29.9052 42.0387 33.2618 39.5667 36.4142 36.4142C39.5667 33.2618 42.0387 29.9052 43.4579 26.9723C44.1631 25.5148 44.659 24.0585 44.7893 22.7217C44.9179 21.4027 44.7097 19.8813 43.5881 18.7597L29.2403 4.41187C27.8527 3.02428 25.8765 3.02573 24.2861 3.36776C22.6081 3.72863 20.7334 4.58419 18.8396 5.74801C16.4978 7.18716 13.9881 9.18353 11.5858 11.5858C9.18354 13.988 7.18717 16.4978 5.74802 18.8396C4.58421 20.7334 3.72865 22.6081 3.36778 24.2861C3.02574 25.8765 3.02429 27.8527 4.41189 29.2403Z"
                  fill="currentColor"
                ></path>
              </svg>
            </div>
            <h2 class="dashboard-header-title">MediCare</h2>
          </div>
          <div class="dashboard-header-right">
            <!-- Mobile hamburger menu button -->
            <button class="dashboard-mobile-menu-btn" id="mobileMenuBtn">
              <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="currentColor" viewBox="0 0 256 256">
                <path d="M224,128a8,8,0,0,1-8,8H40a8,8,0,0,1,0-16H216A8,8,0,0,1,224,128ZM40,72H216a8,8,0,0,0,0-16H40a8,8,0,0,0,0,16ZM216,184H40a8,8,0,0,0,0,16H216a8,8,0,0,0,0-16Z"></path>
              </svg>
            </button>
            
            <div class="dashboard-nav">
              <a class="dashboard-nav-link active" href="/receptionist/dashboard">Dashboard</a>
              <a class="dashboard-nav-link" href="/receptionist/appointments">Appointments</a>
              <a class="dashboard-nav-link" href="/receptionist/patients">Patients</a>
              <a class="dashboard-nav-link" href="/receptionist/billing">Billing</a>
              <a class="dashboard-nav-link" href="/receptionist/reports">Reports</a>
              <a class="dashboard-nav-link" href="/receptionist/messages">Messages</a>
            </div>
            <button class="dashboard-notification-btn">
              <div>
                <svg xmlns="http://www.w3.org/2000/svg" width="20px" height="20px" fill="currentColor" viewBox="0 0 256 256">
                  <path
                    d="M221.8,175.94C216.25,166.38,208,139.33,208,104a80,80,0,1,0-160,0c0,35.34-8.26,62.38-13.81,71.94A16,16,0,0,0,48,200H88.81a40,40,0,0,0,78.38,0H208a16,16,0,0,0,13.8-24.06ZM128,216a24,24,0,0,1-22.62-16h45.24A24,24,0,0,1,128,216ZM48,184c7.7-13.24,16-43.92,16-80a64,64,0,1,1,128,0c0,36.05,8.28,66.73,16,80Z"
                  ></path>
                </svg>
              </div>
            </button>
            <div class="dashboard-avatar"></div>
          </div>
        </header>
        <div class="dashboard-main">
          <div class="dashboard-content-container">
            <div class="dashboard-page-header">
              <div class="dashboard-header-content">
                <p class="dashboard-page-title">Receptionist Dashboard</p>
                <p class="dashboard-page-subtitle">Manage today's appointments and patient check-ins.</p>
              </div>
            </div>
            <h2 class="dashboard-section-title">Real-Time Overview</h2>
            <div class="dashboard-stats-container">
              <div class="dashboard-stat-card">
                <p class="dashboard-stat-label">Check-Ins</p>
                <p class="dashboard-stat-number"><%= typeof checkIns !== 'undefined' ? checkIns : '15' %></p>
                <p class="dashboard-stat-change positive">+5%</p>
              </div>
              <div class="dashboard-stat-card">
                <p class="dashboard-stat-label">Cancellations</p>
                <p class="dashboard-stat-number"><%= typeof cancellations !== 'undefined' ? cancellations : '2' %></p>
                <p class="dashboard-stat-change negative">-1%</p>
              </div>
              <div class="dashboard-stat-card">
                <p class="dashboard-stat-label">Status Updates</p>
                <p class="dashboard-stat-number"><%= typeof statusUpdates !== 'undefined' ? statusUpdates : '10' %></p>
                <p class="dashboard-stat-change positive">+3%</p>
              </div>
            </div>
            <div class="dashboard-actions">
              <div class="dashboard-action-buttons">
                <button class="dashboard-btn dashboard-btn-secondary">
                  <span>Last Updated: <%= new Date().toLocaleTimeString('en-US', { hour: '2-digit', minute: '2-digit' }) %></span>
                </button>
                <button class="dashboard-btn dashboard-btn-primary" id="refreshBtn">
                  <span>Refresh</span>
                </button>
              </div>
            </div>
            <h2 class="dashboard-section-title">Today's Appointments</h2>
            <div class="dashboard-filters">
              <button class="dashboard-filter-btn">
                <p class="dashboard-filter-text">Doctor</p>
                <div>
                  <svg xmlns="http://www.w3.org/2000/svg" width="20px" height="20px" fill="currentColor" viewBox="0 0 256 256">
                    <path d="M213.66,101.66l-80,80a8,8,0,0,1-11.32,0l-80-80A8,8,0,0,1,53.66,90.34L128,164.69l74.34-74.35a8,8,0,0,1,11.32,11.32Z"></path>
                  </svg>
                </div>
              </button>
              <button class="dashboard-filter-btn">
                <p class="dashboard-filter-text">Room</p>
                <div>
                  <svg xmlns="http://www.w3.org/2000/svg" width="20px" height="20px" fill="currentColor" viewBox="0 0 256 256">
                    <path d="M213.66,101.66l-80,80a8,8,0,0,1-11.32,0l-80-80A8,8,0,0,1,53.66,90.34L128,164.69l74.34-74.35a8,8,0,0,1,11.32,11.32Z"></path>
                  </svg>
                </div>
              </button>
              <button class="dashboard-filter-btn">
                <p class="dashboard-filter-text">Service Type</p>
                <div>
                  <svg xmlns="http://www.w3.org/2000/svg" width="20px" height="20px" fill="currentColor" viewBox="0 0 256 256">
                    <path d="M213.66,101.66l-80,80a8,8,0,0,1-11.32,0l-80-80A8,8,0,0,1,53.66,90.34L128,164.69l74.34-74.35a8,8,0,0,1,11.32,11.32Z"></path>
                  </svg>
                </div>
              </button>
            </div>
            <div class="dashboard-table-section">
              <div class="dashboard-table-wrapper">
                <table class="dashboard-table">
                  <thead>
                    <tr>
                      <th class="dashboard-table-col-1">Patient Name</th>
                      <th class="dashboard-table-col-2">Appointment Time</th>
                      <th class="dashboard-table-col-3">Service Type</th>
                      <th class="dashboard-table-col-4">Doctor</th>
                      <th class="dashboard-table-col-5">Room</th>
                      <th class="dashboard-table-col-6">Status</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% if (typeof appointments !== 'undefined' && appointments.length > 0) { %>
                      <% appointments.forEach(function(appointment) { %>
                        <tr>
                          <td class="dashboard-table-col-1 patient-name">
                            <%= appointment.patientName %>
                          </td>
                          <td class="dashboard-table-col-2 appointment-info"><%= appointment.appointmentTime %></td>
                          <td class="dashboard-table-col-3 appointment-info">
                            <%= appointment.serviceType %>
                          </td>
                          <td class="dashboard-table-col-4 appointment-info">
                            <%= appointment.doctorName %>
                          </td>
                          <td class="dashboard-table-col-5 appointment-info"><%= appointment.room %></td>
                          <td class="dashboard-table-col-6">
                            <button class="dashboard-status-btn">
                              <span><%= appointment.status %></span>
                            </button>
                          </td>
                        </tr>
                      <% }); %>
                    <% } else { %>
                      <!-- Sample data when no appointments are provided -->
                      <tr>
                        <td class="dashboard-table-col-1 patient-name">
                          Clara Bennett
                        </td>
                        <td class="dashboard-table-col-2 appointment-info">9:00 AM</td>
                        <td class="dashboard-table-col-3 appointment-info">
                          General Checkup
                        </td>
                        <td class="dashboard-table-col-4 appointment-info">
                          Dr. Olivia Carter
                        </td>
                        <td class="dashboard-table-col-5 appointment-info">Room 101</td>
                        <td class="dashboard-table-col-6">
                          <button class="dashboard-status-btn">
                            <span>Scheduled</span>
                          </button>
                        </td>
                      </tr>
                      <tr>
                        <td class="dashboard-table-col-1 patient-name">
                          Owen Foster
                        </td>
                        <td class="dashboard-table-col-2 appointment-info">9:30 AM</td>
                        <td class="dashboard-table-col-3 appointment-info">
                          Dental Cleaning
                        </td>
                        <td class="dashboard-table-col-4 appointment-info">
                          Dr. Nathan Evans
                        </td>
                        <td class="dashboard-table-col-5 appointment-info">Room 202</td>
                        <td class="dashboard-table-col-6">
                          <button class="dashboard-status-btn">
                            <span>Checked In</span>
                          </button>
                        </td>
                      </tr>
                    <% } %>
                  </tbody>
                </table>
              </div>
            </div>
            <h2 class="dashboard-section-title">Check-In Queue</h2>
            <div class="dashboard-table-section">
              <div class="dashboard-table-wrapper">
                <table class="dashboard-table">
                  <thead>
                    <tr>
                      <th class="dashboard-table-col-1">Patient Name</th>
                      <th class="dashboard-table-col-2">Appointment Time</th>
                      <th class="dashboard-table-col-3">Service Type</th>
                      <th class="dashboard-table-col-4">Doctor</th>
                      <th class="dashboard-table-col-5">Room</th>
                      <th class="dashboard-table-col-6 action-header">Action</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% if (typeof checkInQueue !== 'undefined' && checkInQueue.length > 0) { %>
                      <% checkInQueue.forEach(function(patient) { %>
                        <tr>
                          <td class="dashboard-table-col-1 patient-name">
                            <%= patient.patientName %>
                          </td>
                          <td class="dashboard-table-col-2 appointment-info"><%= patient.appointmentTime %></td>
                          <td class="dashboard-table-col-3 appointment-info">
                            <%= patient.serviceType %>
                          </td>
                          <td class="dashboard-table-col-4 appointment-info">
                            <%= patient.doctorName %>
                          </td>
                          <td class="dashboard-table-col-5 appointment-info"><%= patient.room %></td>
                          <td class="dashboard-table-col-6 action-cell">
                            <button class="dashboard-action-btn" data-patient-id="<%= patient.id %>">Check In</button>
                          </td>
                        </tr>
                      <% }); %>
                    <% } else { %>
                      <!-- Sample data when no check-in queue is provided -->
                      <tr>
                        <td class="dashboard-table-col-1 patient-name">
                          Liam Walker
                        </td>
                        <td class="dashboard-table-col-2 appointment-info">11:30 AM</td>
                        <td class="dashboard-table-col-3 appointment-info">
                          General Checkup
                        </td>
                        <td class="dashboard-table-col-4 appointment-info">
                          Dr. Emily Carter
                        </td>
                        <td class="dashboard-table-col-5 appointment-info">Room 101</td>
                        <td class="dashboard-table-col-6 action-cell">
                          <button class="dashboard-action-btn">Check In</button>
                        </td>
                      </tr>
                    <% } %>
                  </tbody>
                </table>
              </div>
            </div>
            <h2 class="dashboard-section-title">Missed Appointments (Last 7 Days)</h2>
            <div class="dashboard-table-section">
              <div class="dashboard-table-wrapper">
                <table class="dashboard-table">
                  <thead>
                    <tr>
                      <th class="dashboard-table-col-1">Patient Name</th>
                      <th class="dashboard-table-col-2">Appointment Time</th>
                      <th class="dashboard-table-col-3">Service Type</th>
                      <th class="dashboard-table-col-4">Doctor</th>
                      <th class="dashboard-table-col-5">Room</th>
                      <th class="dashboard-table-col-6 action-header">Action</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% if (typeof missedAppointments !== 'undefined' && missedAppointments.length > 0) { %>
                      <% missedAppointments.forEach(function(appointment) { %>
                        <tr>
                          <td class="dashboard-table-col-1 patient-name">
                            <%= appointment.patientName %>
                          </td>
                          <td class="dashboard-table-col-2 appointment-info"><%= appointment.appointmentTime %></td>
                          <td class="dashboard-table-col-3 appointment-info">
                            <%= appointment.serviceType %>
                          </td>
                          <td class="dashboard-table-col-4 appointment-info">
                            <%= appointment.doctorName %>
                          </td>
                          <td class="dashboard-table-col-5 appointment-info"><%= appointment.room %></td>
                          <td class="dashboard-table-col-6 action-cell">
                            <button class="dashboard-action-btn" data-appointment-id="<%= appointment.id %>" data-action="rebook">Rebook</button>
                            <button class="dashboard-action-btn" data-appointment-id="<%= appointment.id %>" data-action="add-reason">Add Reason</button>
                            <button class="dashboard-action-btn" data-appointment-id="<%= appointment.id %>" data-action="tag">Tag</button>
                          </td>
                        </tr>
                      <% }); %>
                    <% } else { %>
                      <!-- Sample data when no missed appointments are provided -->
                      <tr>
                        <td class="dashboard-table-col-1 patient-name">
                          Mia Bennett
                        </td>
                        <td class="dashboard-table-col-2 appointment-info">2:00 PM</td>
                        <td class="dashboard-table-col-3 appointment-info">
                          General Checkup
                        </td>
                        <td class="dashboard-table-col-4 appointment-info">
                          Dr. Emily Carter
                        </td>
                        <td class="dashboard-table-col-5 appointment-info">Room 101</td>
                        <td class="dashboard-table-col-6 action-cell">
                          <button class="dashboard-action-btn">Rebook</button>
                          <button class="dashboard-action-btn">Add Reason</button>
                          <button class="dashboard-action-btn">Tag</button>
                        </td>
                      </tr>
                    <% } %>
                  </tbody>
                </table>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Mobile Menu Overlay -->
    <div class="dashboard-mobile-overlay" id="mobileOverlay"></div>

    <script>
    // Mobile menu functionality
    function toggleMobileMenu() {
      try {
        const nav = document.querySelector('.dashboard-nav');
        const overlay = document.getElementById('mobileOverlay');
        const body = document.body;
        
        console.log('Toggle clicked', { nav, overlay });
        console.log('Screen width:', window.innerWidth);
        console.log('Nav display:', window.getComputedStyle(nav).display);
        
        if (nav && overlay) {
          nav.classList.toggle('mobile-open');
          overlay.classList.toggle('active');
          body.classList.toggle('mobile-menu-open');
          console.log('Menu toggled successfully');
          console.log('Nav classes:', nav.className);
          console.log('Overlay classes:', overlay.className);
        } else {
          console.error('Elements not found', { nav, overlay });
        }
      } catch (error) {
        console.error('Error in toggle function:', error);
      }
    }

    // Initialize mobile menu functionality
    document.addEventListener('DOMContentLoaded', function() {
      // Add click event listener to hamburger button
      const mobileMenuBtn = document.getElementById('mobileMenuBtn');
      if (mobileMenuBtn) {
        mobileMenuBtn.addEventListener('click', toggleMobileMenu);
        console.log('Mobile menu button event listener added');
      }

      // Add click event listener to overlay
      const overlay = document.getElementById('mobileOverlay');
      if (overlay) {
        overlay.addEventListener('click', toggleMobileMenu);
        console.log('Overlay event listener added');
      }

      // Close mobile menu when clicking on navigation links
      const navLinks = document.querySelectorAll('.dashboard-nav-link');
      navLinks.forEach(link => {
        link.addEventListener('click', () => {
          const nav = document.querySelector('.dashboard-nav');
          const overlay = document.getElementById('mobileOverlay');
          const body = document.body;
          
          if (nav && overlay && nav.classList.contains('mobile-open')) {
            nav.classList.remove('mobile-open');
            overlay.classList.remove('active');
            body.classList.remove('mobile-menu-open');
          }
        });
      });

      // Close mobile menu on escape key
      document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape') {
          const nav = document.querySelector('.dashboard-nav');
          const overlay = document.getElementById('mobileOverlay');
          const body = document.body;
          
          if (nav && overlay && nav.classList.contains('mobile-open')) {
            nav.classList.remove('mobile-open');
            overlay.classList.remove('active');
            body.classList.remove('mobile-menu-open');
          }
        }
      });

      // Add event listeners for dashboard action buttons
      const refreshBtn = document.getElementById('refreshBtn');
      if (refreshBtn) {
        refreshBtn.addEventListener('click', () => {
          location.reload();
        });
      }

      // Add event listeners for check-in buttons
      const checkInButtons = document.querySelectorAll('.dashboard-action-btn[data-patient-id]');
      checkInButtons.forEach(button => {
        button.addEventListener('click', () => {
          const patientId = button.getAttribute('data-patient-id');
          if (patientId) {
            checkInPatient(patientId);
          }
        });
      });

      // Add event listeners for action buttons
      const actionButtons = document.querySelectorAll('.dashboard-action-btn');
      actionButtons.forEach(button => {
        button.addEventListener('click', () => {
          const appointmentId = button.getAttribute('data-appointment-id');
          const action = button.getAttribute('data-action');
          
          if (appointmentId && action) {
            switch (action) {
              case 'rebook':
                rebookAppointment(appointmentId);
                break;
              case 'add-reason':
                addReason(appointmentId);
                break;
              case 'tag':
                tagPatient(appointmentId);
                break;
            }
          }
        });
      });

      console.log('Mobile menu initialization complete');
    });

    // Dashboard action functions
    function checkInPatient(patientId) {
      console.log('Checking in patient:', patientId);
      // Add your check-in logic here
    }

    function rebookAppointment(appointmentId) {
      console.log('Rebooking appointment:', appointmentId);
      // Add your rebook logic here
    }

    function addReason(appointmentId) {
      console.log('Adding reason for appointment:', appointmentId);
      // Add your add reason logic here
    }

    function tagPatient(appointmentId) {
      console.log('Tagging patient for appointment:', appointmentId);
      // Add your tag logic here
    }
    </script>
    <script src="/js/receptionist-dashboard.js"></script>
  </body>
</html> 